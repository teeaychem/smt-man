#pragma once

/// Cardinal directions
enum direction_e {
  NORTH,
  EAST,
  SOUTH,
  WEST,
};
typedef enum direction_e Direction;

/// Quadrants of a circle
enum quadrant_e {
  FIRST,
  SECOND,
  THIRD,
  FOURTH,
};
typedef enum quadrant_e Quadrant;
